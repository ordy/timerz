import { Appbar } from 'react-native-paper';

const Navbar = () => (
	<Appbar.Header>
		<Appbar.Content title='Timerz' />
		<Appbar.Action icon='dots-vertical' onPress={() => {}} />
	</Appbar.Header>
);

export default Navbar;
